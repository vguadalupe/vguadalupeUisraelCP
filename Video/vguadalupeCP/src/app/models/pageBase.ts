export declare interface PageBase {
  /**
   * Parámetro para loading de page
   */
  loading: boolean;

  /**
   * Parámetro título de página
   */
  titlePage: string;
  /**
   * Mensaje de error
   */
  errorMessage: string;
}
